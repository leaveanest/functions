---
description: html-lp-edit-prompt
globs: *
alwaysApply: false
---
# HTMLランディングページ編集支援プロンプト

## 基本情報

```
HTMLランディングページの編集支援が必要です。

【現在の状況】
- 編集したいファイル: [ファイルパスを記入]
- 現在のコード: [該当コードを貼り付け]

【達成したい目標】
- [明確な目標を記述]

【制約条件】
- [デザイン要件、ブランドガイドライン、技術的制約など]
```

## 特定タスク用プロンプト

### HTML構造の編集

```
以下のHTMLセクションを編集したいです：

```html
[現在のHTMLコード]
```

変更内容：
- [必要な変更を箇条書きで]

SEO考慮事項：
- [必要に応じて記入]
```

### CSSスタイリングの編集

```
以下のCSSを改善したいです：

```css
[現在のCSSコード]
```

達成したいスタイル：
- [視覚的な目標]

レスポンシブ対応：
- [モバイル対応などの要件]
```

### JavaScriptの追加・修正

```
以下の機能を実装したいです：

現在のJSコード（ある場合）：
```javascript
[既存のJavaScriptコード]
```

実装したい機能：
- [機能の詳細説明]
```

### パフォーマンス最適化

```
以下のコードのパフォーマンスを改善したいです：

```html
[対象コード]
```

現在の問題点：
- [遅延、読み込み問題など]
```

### アクセシビリティ改善

```
以下のセクションのアクセシビリティを向上させたいです：

```html
[対象コード]
```

改善ポイント：
- [スクリーンリーダー対応、キーボードナビゲーションなど]
```

## 使用例

```
HTMLランディングページの編集支援が必要です。

【現在の状況】
- 編集したいファイル: index.html
- 現在のコード:
```html
<div class="hero-section">
  <h1>ようこそ</h1>
  <p>私たちのサービスについて</p>
  <button>詳細を見る</button>
</div>
```

【達成したい目標】
- ヒーローセクションをより魅力的にし、CTAボタンの視認性を向上させる

【制約条件】
- 企業カラー（#3366cc, #ff9900）を使用すること
- モバイルデバイスでも適切に表示されること
```

## 画像・メディアの最適化

```
以下の画像関連コードを最適化したいです：

```html
[画像関連のHTML]
```

最適化目標：
- [ロード時間短縮、レスポンシブ画像など]

追加したい機能：
- [遅延読み込み、WebP対応など]
```

## フォーム実装・改善

```
以下のフォームを改善したいです：

```html
[フォームのHTML]
```

改善点：
- [バリデーション、UX改善など]

必要な機能：
- [送信処理、エラー表示など]
```

## アニメーション・インタラクション

```
以下の要素にアニメーションを追加したいです：

```html
[対象要素]
```

期待する動作：
- [スクロール時のエフェクト、ホバーアニメーションなど]

実装方法の希望：
- [CSS/JS、ライブラリ使用有無など]
``` 
